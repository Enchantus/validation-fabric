# Agent Guidance

This repository is public. Never commit secrets, credentials, private logs, PHI, host facts, or SessionBuddie product configuration.

Use issue-linked `codex/<issue>-slug` branches and pull requests. Keep pull-request execution unprivileged. Admission and merge authority must execute only from code trusted on the default branch. Add focused tests for every trust-boundary or compatibility change.
